const contractAddresses = require("./contractAddresses.json")
const abi = require("./abi.json")

module.exports = {
    contractAddresses,
    abi,
}

// dùng để import bên src\components\LotteryEntrance.jsx
    // (import bằng từ khóa "import")